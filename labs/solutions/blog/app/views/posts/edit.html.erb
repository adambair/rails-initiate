<h1>Editing post</h1>

<% form_for(@post) do |f| %>
  <%= render :partial => 'post_fields', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
